# Integration Tests

This folder is reserved for integration tests.
Integration tests verify the extension's behavior by loading the compiled `fbvector` plugin into a running Firebird server instance, executing queries, and validating outputs.
